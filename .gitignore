#=========================================================================
# Git Ignore Files
#=========================================================================
# We explicitly ignore a default build directory, the autoconf generated
# autom4te.cache directory, and automatically generated python files.
# This makes it easy to do a clean build under the source directory and
# still have it appear clean to git.

.\#*
*build*/
autom4te.cache*
admin/work*
.DS_Store
.cache/
*~
.lock*
*.swp
*.log
*-adhoc
*-test
*-sim
*.vcd
.vscode/
.vscode/*
*__pycache__

# hack for now -cbatten
admin/groups-all.txt
admin/groups-rest.txt
admin/groups-short.txt
admin/groups.txt
admin/repos-all.txt
admin/repos.txt
admin/netids.txt

